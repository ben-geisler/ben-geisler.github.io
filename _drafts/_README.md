# `_drafts`

Empty on purpose. Unpublished blog post drafts live here, without a date
prefix in the filename (for example `an-idea.md`). Drafts are excluded from a
normal build; preview them with `bundle exec jekyll serve --drafts`. Move a
file to `_posts/` and rename it `YYYY-MM-DD-slug.md` to publish it.
